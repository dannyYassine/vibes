# Required — absolute path to the directory the agent can read/write
AGENT_WORKSPACE=/path/to/your/workspace

# Optional — defaults shown
LM_STUDIO_URL=http://localhost:1234/v1
MODEL_NAME=qwen3.5-4b-instruct
